# Деплой Rockgarden

## Вариант 1: Vercel (Рекомендуется) 🚀

### Быстрый старт (CLI):
```bash
# Установите Vercel CLI
npm i -g vercel

# Зайдите в папку проекта
cd safe-journal

# Деплой (запустит билд и даст URL)
vercel --prod
```

### Через веб-интерфейс:
1. Зарегистрируйтесь на [vercel.com](https://vercel.com) (можно через GitHub)
2. Нажмите "Add New Project"
3. Импортируйте репозиторий (нужно дать Vercel доступ к приватному репо)
4. Настройки:
   - **Framework Preset**: Vite
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
5. Нажмите Deploy

### Преимущества Vercel:
- ⚡ Быстрый CDN по всему миру
- 🔄 Автодеплой при каждом push
- 🔒 HTTPS по умолчанию
- 📱 Превью для каждого PR

---

## Вариант 2: Netlify (Проще) 🌐

### Drag & Drop (без регистрации):
```bash
# Соберите проект локально
cd safe-journal
npm run build

# Загрузите папку dist на netlify.com/drop
```

### Через Git:
1. Зарегистрируйтесь на [netlify.com](https://netlify.com)
2. "Add new site" → "Import an existing project"
3. Выберите репозиторий
4. Настройки:
   - **Build command**: `npm run build`
   - **Publish directory**: `dist`
5. Deploy

---

## Вариант 3: GitHub Pages (Если сделать репо публичным) 📄

Репозиторий должен быть **публичным** для бесплатного использования GitHub Pages.

### Включение Pages:
1. Зайдите в репозиторий на GitHub
2. Settings → Pages (в левом меню)
3. Source: GitHub Actions
4. Workflow уже настроен в `.github/workflows/deploy.yml`
5. Сделайте push — сайт соберётся автоматически

URL будет: `https://your-username.github.io/repository-name/`

⚠️ **Важно**: Для приватных репо GitHub Pages платный ($4/месяц). 

---

## Вариант 4: Локальный просмотр 💻

Если хотите просто посмотреть без деплоя:

```bash
cd safe-journal
npm run build
npm run preview
```

Откроется на `http://localhost:4173`

---

## Рекомендация

Для тестирования используйте **Netlify Drop** (вариант 2) — быстро, не нужна регистрация, просто перетащите папку `dist`.

Для постоянного использования — **Vercel** (вариант 1) — быстрее работает, автообновления.

---

## Проверка перед деплоем

```bash
# Убедитесь, что всё собирается без ошибок
npm run build

# Проверьте типы
npm run type-check

# Запустите тесты
npm test
```

Все проверки прошли? Можно деплоить! 🚀
